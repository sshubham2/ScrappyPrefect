Add .env and serviceAccount.json files to environment folder:
/environment/.env
/src

Login to prefect cloud to register flow or start prefect orion server locally.
Start a flow by executing python flowname.py
